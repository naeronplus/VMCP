# E013 — FENCING_TOKEN_REJECTED

Presented fencing token does not match the latest Postgres `lock_fencing_seq` entry for the lock owner, or the token's `instanceId` no longer matches the current Redis master after failover.

Workers must re-acquire the lock and obtain a new token before committing.
